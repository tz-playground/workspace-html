{
  "name": "workspace-html",
  "version": "1.0.0",
  "description": "A project to demonstrate static HTML page with Vite",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --no-open"
  },
  "devDependencies": {
    "vite": "^2.9.18"
  }
}
